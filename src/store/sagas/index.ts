import {all, put, takeEvery} from "redux-saga/effects";

// import {saga} from "./saga-path";

export default function* rootSaga() {
    yield all([
        // yield takeEvery('action', saga),
    ]);
}
