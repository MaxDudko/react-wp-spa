import {combineReducers} from 'redux';

// import {reducer, IReducerState} from "./reducer-path";

export const reducers = combineReducers({
    // reducer,
});

export interface IReduxState {
    // reducer: IReducerState,
}
